//8kyu `Convert a Boolean to a String`

//Implement a function which convert the given boolean value into its string representation.

//Note: Only valid inputs will be given.

//P boolean
//R string
//E boolean true -> "true"; boolean false -> "false"
//P there is probably a method for this

let booleanToString = (b) => {
    if(b == true){
      return "true"
    }else if ( b == false){
      return "false"
    }
  }